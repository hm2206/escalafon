<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    {{ style(link + "/css/default.css") }}
    {{ style(link + "/css/report.css") }}
    <title>Reporte de Ficha Escalafonaria</title>
</head>

<body class="a4">

    @each(data in datos)
        {{--  type  title --}}
        @if(data.key == 'TITLE') 
            <h3 class="text-center mt-1 uppercase">{{ data.current }}</h3>
            <br/>
        @endif 
        {{--  type job  --}}
        @if(data.key == 'JOB')
            <br/>
            <table class="table f-11" width="100%" style="page-break-inside: avoid;">
                <thead>
                    <tr>
                        <th width="120px" class="text-left">Trabajador</th>
                        <td class="uppercase">: {{ data.current.fullname || "" }}</td>
                    </tr>
                    <tr>
                        <th width="120px" class="text-left">Categoría</th>
                        <td class="uppercase">: {{ data.current.categoria || "" }}</td>
                    </tr>
                </thead>
            </table>
            <br/>
        @endif
        {{--  type fecha  --}}
        @if(data.key == 'FECHA')
            <table class="table f-11" width="100%" style="page-break-inside: avoid;">
                <thead>
                    <tr>
                        <th style="border-bottom: 1.5px solid #000;" colspan="2" width="50px" class="text-left">
                            Fecha: <span class="ml-2 pl-2">{{ data.current.fecha }}</span>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @each(assistance in data.current.assistances)
                        <tr>
                            <td width="200px">
                                <span class="pl-3">{{ assistance.modo || "" }}</span>
                            </td>
                            <td>
                                {{ assistance.time }}  <span class="ml-2">{{ assistance.status }}</span>
                            </td>
                        </tr>
                    @endeach
                </tbody>
            </table>
        @endif
    @endeach
    
</body>
</html>